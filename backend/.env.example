DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=quiz_platform
JWT_SECRET=your_super_secret_key_here
PORT=5000

# Optional: restrict CORS in production (e.g. https://your-frontend.com)
# CORS_ORIGIN=https://your-frontend.com

# Optional: master admin (if not set, defaults to test@admin.com / admin123 for dev only)
# MASTER_ADMIN_EMAIL=admin@yourdomain.com
# MASTER_ADMIN_PASSWORD=secure_password
